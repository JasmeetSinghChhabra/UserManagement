export class UserNotification {
    Projectid: string;
    ModuleId: number;
    CalleeUserId: string;
    CallerUserId: string;
    CallerName: string;
    AuxParameters: string; // Any additional parameters related to specific modules or custom implementations in JSON string format. 
}
